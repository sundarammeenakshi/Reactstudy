import Upload from "./upload";

function Split(){
    return(
        <Upload  title="Split PDF file" content="Separate one page or a whole set for easy conversion into independent PDF files."/>
    )
}

export default Split;