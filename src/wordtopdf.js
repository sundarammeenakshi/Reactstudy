import Upload from "./upload";

function WordtoPDF(){
    return(
        <Upload  title="Convert WORD to PDF" content="Make DOC and DOCX files easy to read by converting them to PDF."/>
    )
}

export default WordtoPDF;